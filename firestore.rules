rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Această linie permite oricui este logat să scrie și să citească
      allow read, write: if request.auth != null;
    }
  }
}